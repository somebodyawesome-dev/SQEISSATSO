const {
  ajout,
  mettre,
  obtenir,
  supprimer,
  ajoutAvecCleAleatoire,
} = require("../../configs/dao");
/**
 *
 * @param {import('express').Request} req
 * @param {import('express').Response} res
 * @param {Function} next
 */
const ajoutFiliere = (req, res, next) => {};
